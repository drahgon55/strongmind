add Pizza

- popup with name box and list of toppings on the side
- click topping and it gets added
- add each topping to a map to prevent dups, can click topping multiple times added only once
- delete button to remove toppings, maybe might be optional for new. can just cancel and redo
- clicking and existing pizza opens same popup and lets you add more toppings, change the name and maybe delete a topping
- map to array before sending
- backend find dups in request
    - loop through and assign to map value is a counter. If counter > 1 stop and return 400 cant have duplicate toppings
    - add a test


add toppings

not sure yet. Thinking maybe we do a 